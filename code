#r=Number of rows,c=number of columns,#r,c are odd numbers
r, c = map(int, input().split(" "))
x = round(r/2)+1
for i in range(1, r, 2):
    print(((".|.")*i).center(c, "-"))

print(("WELCOME").center(c, "-"))

for i in range(x, -1, -2):
    print(((".|.")*i).center(c, "-"))
